/**
 * Main application configuration
 */
module.exports = {
    /**
     * Paths to required folders
     */
    requiredFolders: [
        "logs",
        "statics"
    ],

    /**
     * Express server configuration
     */
    expressServer: {
        port: 8080
    }
}